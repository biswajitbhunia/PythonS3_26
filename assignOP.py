# Basic assignment
score = 10  # "Put the number 10 inside the box named 'score'"

# Updating the score (Long way)
score = score + 5  

# Updating the score (Python shortcut way)
score += 5  # "Add 5 to whatever the score currently is"
print(score) # Output: 20